---
import Icon from './Icon.astro';
interface Props { href: string; class?: string; }
const { href, class: className } = Astro.props;
---
<a href={href} target="_blank" rel="noopener noreferrer" class:list={['external-link', className]}><slot /><Icon kind="external" /></a>
